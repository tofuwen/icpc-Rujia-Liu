对于二叉树，我果然还是不太熟悉···

从中序遍历和后序遍历建树就做了我半天···
而且取数据还挺麻烦的····

不过我这次建树没有用指针，用的是数组。
lch，rch指向子树在数组中的位置。

妈的，错出翔了····

找了半天，终于找到了第一个错误的地方：数据取的就有问题啊！！！
怪不得makeTree不对呢，因为数据取错了，所以有重复的数据了，find函数死了！！

我靠，原来只是一个笔误···
复制的时候忘记把inorder改为postorder了！！！

靠，查了好长好长时间···

然后果不其然的TLE了···
不过至少咱们建树建对了！对我来说真的是不易了···

我的算法没有错！！！！

TLE的原因是调用了太多次strlen(s)!!!

直接先d = strlen(s)!!

少调用这么些次函数···

唉，TLE，查了半天···