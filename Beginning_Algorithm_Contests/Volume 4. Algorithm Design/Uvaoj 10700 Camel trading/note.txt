SB了！

想到会有可能溢出int，没有改···

果然导致了一次WA···

原理：a+b*c=<（a+b）*c c=1时相等；也可以这么理解，原本*的优先级就大于+，
（a+b）+c，（a*b）+c，等于没加，加括号的改变最后结果的是a*（b+c）这种类型。

标准解法当然是用栈模拟···

只不过求最大值的时候把 + 定义优先级高就行了···

当然我这样递归也行，就是效率不高···

但是n只有12，足够了···