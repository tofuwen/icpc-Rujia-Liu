for(int i=0;i<n;i++)
		{
			for(int j=0;j<g[i].size();j++)
			{
				int ans = min(father(i),father(g[i][j]));
				fa[i] = fa[g[i][j]] = ans;
			}
		}

这个算法并不能把已有的连通分量的father数组都统一起来。

比如1,2,；2,3；3,0

我没仔细想，抖了个机灵，把上述代码复制了一遍，以为这样就行了···
还不行啊！还是上个例子···

好像这个代码执行mn次应该保证没问题！
（真的是诶，AC了···无语！！）

不过这样初始化也太麻烦了，还不如dfs判断连通分量，然后再一次赋值呢···

一个更好的办法：把那些已经存在的边赋值为0，然后就可以直接Kruskal了···